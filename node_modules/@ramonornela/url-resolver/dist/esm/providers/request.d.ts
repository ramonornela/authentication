import { Request as RequestAngular, URLSearchParams } from '@angular/http';
import { Resolve } from './resolve';
import { Metadata } from './metadata/metadata';
export declare class Request {
    protected resolve: Resolve;
    protected defaultOptions: any;
    constructor(resolve: Resolve);
    getResolve(): Resolve;
    getMetadata(): Metadata;
    create(id: string, params?: Object, options?: any): RequestAngular;
    setDefaultOptions(options: any): void;
    protected serializeParams(id: string, options: any, params: Object): void;
    protected createSearchParams(params: Object): URLSearchParams;
}
